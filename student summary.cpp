#include<iostream>
//#define MAX 10
using namespace std;
/*class student
{
       private:
               int roll;
               int marks[MAX];
       public:
              
              float compute(int *m,int sub)
              {
                          int tot=0;
                          for(int i=0;i<sub;i++)
                           tot+=m[i];
                           return tot;
                                  
              }      
              void perc(int *m,int sub)
              { int a=0,b=0,d=0,f=0;
                         float perc=compute(m,sub)/sub;
                         if(perc<40)
                         f++;
                         else if(perc<60)
                         b++;
                         else if(perc<80)
                         a++;
                         else 
                         d++;
              }
};*/
/*int student::a=0;
int student::b=0;
int student::d=0;
int student::f=0;*/
 main()
{  
          //student st[MAX];
         // int n,s;
          //int m[MAX];
          cout<<"students";
          //cin>>s;
          //cout<<"subjects";
          //cin>>n;
          /*for(int i=0;i<s;i++)
          {
                 for(int j=0;j<n;j++)
                 cin>>m[j];
                 st[i].perc(m,n);
          }*/
          //cout<<student::d;
}
